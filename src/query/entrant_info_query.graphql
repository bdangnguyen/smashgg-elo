query EventUserId($event_id:ID!, $page:Int) {
  event(id:$event_id) {
    entrants(query: {page: $page}) {
      nodes {
        id
        participants {
          gamerTag
          user {
            id
          }
        }
      }
    }
  }
}